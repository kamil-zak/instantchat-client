import styled from 'styled-components';

const Input = styled.input`
  width: 250px;
  height: 27px;
  border: none;
  border-bottom: 2px solid rgb(47, 38, 167);
  &:focus {
    outline: none;
  }
`;

export default Input;
